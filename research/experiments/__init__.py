"""Self-contained research experiments (driver scripts + their data layer).

Each subpackage is one reproducible study that REUSES the canonical research
libraries (``data``, ``features``, ``models``, ``envs``) rather than
reimplementing them (CLAUDE.md §20.2).
"""
