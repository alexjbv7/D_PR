[pytest]
asyncio_mode = auto
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Tests are run per-service via Makefile (see make test)
# Direct run: cd services/<svc> && PYTHONPATH=. pytest tests/
addopts = --import-mode=importlib
