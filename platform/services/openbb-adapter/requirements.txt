# OpenBB Platform 4.x — core + providers (sin versiones pinneadas de providers)
openbb==4.7.1
openbb-fred
openbb-yfinance
openbb-fmp
openbb-sec

# Web framework
# OpenBB Platform 4.7.x resolves openbb-core 1.6.x, which requires
# FastAPI >=0.128. Keep this service isolated from the older microservice pins.
fastapi>=0.128.0,<0.129.0
uvicorn[standard]>=0.40.0,<0.41.0
pydantic-settings==2.3.0

# Kafka
aiokafka==0.11.0

# Redis
aioredis==2.0.1

# Scheduler
apscheduler==3.10.4

# HTTP client
httpx==0.27.0

# Observabilidad
structlog==24.1.0

# Shared lib (instalada como editable desde raíz del monorepo)
# pip install -e shared/
