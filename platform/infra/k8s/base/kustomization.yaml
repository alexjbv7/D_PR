apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: los-ojos

resources:
  - namespace.yaml
  - configmap.yaml
  - services.yaml
  - deployments.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/part-of: los-ojos-platform
  app.kubernetes.io/managed-by: kustomize
